========================
BUILD OUTPUT DESCRIPTION
========================

To run the project from the command line, type the following in Command Prompt:

java -jar "EmployFast.jar" 

Alternatively, click on the EmployFast.bat file
